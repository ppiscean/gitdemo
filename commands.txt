Commands:

git config --global user.name "ppiscean"
git config --global user.email ppiscean@yahoo.com

git config --list
	
git remote add origin https://github.com/ppiscean/gitdemo.git
git status
git add .
git commit -m "test commit"
git push origin master
git push 

git checkout --
git revert
git reset 


git branch
git branch abc
git checkout abc
git add .
git commit -m "new version"
git push origin abc